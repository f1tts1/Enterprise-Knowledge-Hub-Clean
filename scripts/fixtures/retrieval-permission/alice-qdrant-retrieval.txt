Qdrant 是一个向量数据库，用于保存文本 chunk 的 embedding 向量和元数据。

在当前项目中，Python 会把文档 chunk、vector、ownerUserId、kbId、docId、pageNo、chunkIndex、text 等 payload 写入 Qdrant。检索时必须使用 ownerUserId 和 kbId 过滤，确保用户只能搜索自己知识库中的文档片段。

关键词：Qdrant、向量检索、payload filter、ownerUserId、kbId、权限隔离。
